# Vite carga .env.production en build y .env.development en `npm run dev`.
# Produccion (servido por nginx en el droplet, mismo dominio):
#   VITE_API_URL=/api
# Dev local (pega al backend del droplet, que permite CORS localhost):
#   VITE_API_URL=https://mingup.duckdns.org/api
